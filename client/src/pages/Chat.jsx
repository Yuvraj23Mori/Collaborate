// src/pages/Chat.js
import React from 'react';
import { Container, Typography } from '@mui/material';

const Chat = () => {
  return (
    <Container maxWidth="md">
      <Typography variant="h4">Welcome to the Chat Room</Typography>
      <p>This is where the chat interface will go.</p>
    </Container>
  );
};

export default Chat;
